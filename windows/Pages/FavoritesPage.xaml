<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="bilibili_music_player_windows.Pages.FavoritesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:models="using:bilibili_music_player_windows.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    NavigationCacheMode="Enabled">

    <Grid Margin="32,32,32,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal" Spacing="12">
                <Rectangle Width="4" Height="24" Fill="{StaticResource Rose400}" VerticalAlignment="Center" RadiusX="2" RadiusY="2" />
                <TextBlock Text="我的收藏" FontSize="28" FontWeight="Bold" Foreground="{StaticResource Slate800}" />
            </StackPanel>
            <TextBlock Grid.Column="1" Text="= 排序" FontSize="14" Foreground="{StaticResource Slate500}" VerticalAlignment="Bottom" />
        </Grid>

        <Border Margin="0,24,0,0" BorderBrush="{StaticResource Slate200}" BorderThickness="0,0,0,1" Padding="16,8" Grid.Row="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="48" />
                    <ColumnDefinition Width="16" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="200" />
                    <ColumnDefinition Width="100" />
                    <ColumnDefinition Width="48" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="#" Foreground="{StaticResource Slate500}" HorizontalAlignment="Center" />
                <TextBlock Grid.Column="2" Text="标题" Foreground="{StaticResource Slate500}" />
                <TextBlock Grid.Column="3" Text="UP主" Foreground="{StaticResource Slate500}" />
                <StackPanel Grid.Column="4" Orientation="Horizontal" Spacing="4">
                    <SymbolIcon Symbol="Clock" Foreground="{StaticResource Slate500}" />
                    <TextBlock Text="时长" Foreground="{StaticResource Slate500}" />
                </StackPanel>
            </Grid>
        </Border>

        <ListView Grid.Row="2" Margin="0,8,0,0" BorderThickness="0" SelectionMode="None" ItemsSource="{x:Bind ViewModel.Favorites, Mode=OneWay}">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:TrackItem">
                    <Grid Padding="16,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="48" />
                            <ColumnDefinition Width="16" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="200" />
                            <ColumnDefinition Width="100" />
                            <ColumnDefinition Width="48" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="{x:Bind Index, Mode=OneWay}" Foreground="{StaticResource Rose400}" HorizontalAlignment="Center" FontWeight="SemiBold" />
                        <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="16">
                            <Border Width="96" Height="56" Background="{StaticResource Slate200}" CornerRadius="6" />
                            <TextBlock Text="{x:Bind Title, Mode=OneWay}" Foreground="{StaticResource Slate800}" TextTrimming="CharacterEllipsis" VerticalAlignment="Center" />
                        </StackPanel>
                        <TextBlock Grid.Column="3" Text="{x:Bind Artist, Mode=OneWay}" Foreground="{StaticResource Slate500}" TextTrimming="CharacterEllipsis" VerticalAlignment="Center" />
                        <TextBlock Grid.Column="4" Text="{x:Bind Duration, Mode=OneWay}" Foreground="{StaticResource Slate500}" FontFamily="Consolas" VerticalAlignment="Center" />
                        <SymbolIcon Grid.Column="5" Symbol="More" Foreground="{StaticResource Slate400}" VerticalAlignment="Center" HorizontalAlignment="Center" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
